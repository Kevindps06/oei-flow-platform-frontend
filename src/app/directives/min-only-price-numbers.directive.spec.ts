import { MinOnlyPriceNumbersDirective } from './min-only-price-numbers.directive';

describe('MinOnlyPriceNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new MinOnlyPriceNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
