import { MaxOnlyPriceNumbersDirective } from './max-only-price-numbers.directive';

describe('MaxOnlyPriceNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxOnlyPriceNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
