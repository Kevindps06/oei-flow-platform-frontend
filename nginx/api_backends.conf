# localhost
#upstream flow_platform_backend {
#    zone backend_localhost_service 64k;
#    server node_localhost;
#}

# preproduction
upstream flow_platform_backend {
    zone backend_preproduction_service 64k;
    server node_preproduction;
}

# production
#upstream flow_platform_backend {
#    zone backend_production_service 64k;
#    server node_production;
#}

# ricuras-premium
upstream ricuras_premium {
    zone ricuras_premium_service 64k;
    server ricuras_premium_wordpress:443;
}

# vim: syntax=nginx